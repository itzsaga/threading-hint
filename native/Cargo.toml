[package]
name = "threading-hint"
version = "0.1.0"
authors = ["Seth <itz.saga@gmail.com>"]
license = "MIT"
build = "build.rs"

[lib]
name = "threading_hint"
crate-type = ["dylib"]

[build-dependencies]
neon-build = "0.1.22"

[dependencies]
neon = "0.1.22"
num_cpus = "1.4.0"
